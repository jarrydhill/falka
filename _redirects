# Cloudflare Pages URL rewrites / redirects
#
# Cloudflare automatically strips `.html` and canonicalises trailing slashes
# BEFORE applying these rules. Rules are evaluated in order; first match wins.
# Files that exist are served directly (rules never consulted).

# --- Legacy 301s for slugs that were renamed in Sanity ---
/falka-01        /vika-8           301
/falka-03        /vika-81          301

# --- Current boat slugs → /boat.html template (rewrite, URL preserved) ---
# Add one line per live boat. When Jarryd adds a new boat to Sanity,
# append its slug here. This avoids a catchall that would swallow
# /login and other non-boat paths.
/vika-8          /boat.html        200
/falka-02        /boat.html        200
/vika-81         /boat.html        200
